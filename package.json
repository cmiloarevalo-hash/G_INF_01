{
  "name": "g-inf-01",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch server.ts",
    "build": "tsc -p tsconfig.server.json && tsc --noEmit -p tsconfig.app.json && vite build && node -e \"import('node:fs').then(fs => { fs.mkdirSync('dist/src/report-types/title-study', { recursive: true }); fs.copyFileSync('src/report-types/title-study/prompt.md', 'dist/src/report-types/title-study/prompt.md'); })\"",
    "start": "node dist/server.js",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "express": "^5.0.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^4.6.5",
    "docx": "^9.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "tsx": "^4.19.2",
    "typescript": "~5.9",
    "vite": "~7.3"
  }
}
